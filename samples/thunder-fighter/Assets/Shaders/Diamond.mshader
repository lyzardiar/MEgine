fn mengine_ui_hook(input: MEngineUiInput) -> vec4<f32> {
    let p=abs((input.uv0-vec2<f32>(0.5))*2.0);
    let d=p.x+p.y;
    return vec4<f32>(mix(input.vertex_color.rgb,vec3<f32>(1.0),exp(-d*d*12.0)),(smoothstep(0.85,0.65,d)+exp(-d*d*3.0)*0.4)*input.vertex_color.a);
}
